首字母expe.. 为什么字符串


列表生成式  ，已经在内存中生成
生成器 推导算法
1.生成器 必须一个一个访问 ，不能跳着去取值  ，  如果取到要的值 ，那之前的值都丢弃了
 一边计算 一边生成  ：generator
    (　）　小括号就是生成器
2.生成一个生成器对象，就是代表推到公式准好了的意思

3.yield a    返回a的值 同时挂起当前这个函数    但是不结束
             a返回给通过__next__()调用当前函数的人
             __next__()  方法就是来调用yield值    当采用yield方法时，就必须用__next__() 来调用取得下一个值
  这代表通过yield 就实现了函数的中断， 并且保存了函数的中间执行状态
4 send(i)   ：send 相当于next方法，但是不同的时 send可以传递数据，，next不能传递数据，只能唤醒