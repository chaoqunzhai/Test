其中 如果赋值函数   不加()  就不执行


迭代器是一个推导算法
迭代器:可以被next() 函数调用并不断返回下一个值的对象成为迭代器:Iterator
所以的生成器一定都是迭代器
迭代器不一定都是生成器，
列表，字典可以迭代  ，但不是迭代器

iter() 方法可以把一个列表，字典，集合变成迭代器     本质上迭代器和生成器一样，只是不同的生成方式

py3中for 循环就是next方法

装饰器的作用：
        完全符合 开放-封闭原则，
        不改变原有功能代码，不改变原有调用方式，然后实现扩展新的功能