做技术没前途？ no ，是你没前途

@classmethod   类方法:只能访问类的公有属性，不能访问实例属性


@property   属性方法，的作用把一个方法变成一个静态属性（把方法变成一个变量）


if __name = '__main__'   ：被导入，但是不执行


类都是用type创建的



__next__ 如果有 就先执行nest方法


主动触发异常 ； 就是对返回值进行判断

断言：异常处理的简写
    assert 1 == 2        必须满足    警示作用    致命错误！
自定义异常

反射，可以在对象中 获取到任何
hasatter(容器，‘名称’)           #以字符串的形式判断某个对象中是否含有指定的属性
getatter(容器，‘名称’)           #以字符串的形式去某个对象获取指定的属性
setatter(容器，‘名称’，值)       #以字符串的形式去某个对象设置指定的属性
delatter(容器，‘名称’)           #以字符串的形式去某个对象删除指定的属性


